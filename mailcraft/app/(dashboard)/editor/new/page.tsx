export const metadata = {
  title: 'New Template — MailCraft',
}

// ?master=<masterTemplateId> — passed when cloning from the browse page
export default function NewEditorPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold mb-2">New Template</h1>
      <p className="text-sm text-muted-foreground">
        Editor — coming in Phase 5
      </p>
    </div>
  )
}
